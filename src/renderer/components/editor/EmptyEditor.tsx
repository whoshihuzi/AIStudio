// ============================================================
// EmptyEditor — shown when no file is open.
// ============================================================

export function EmptyEditor() {
  return (
    <div className="flex-1 flex items-center justify-center text-xs text-gray-600">
      Open a file to edit
    </div>
  );
}
